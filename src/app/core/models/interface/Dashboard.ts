export interface Card {
  background: string,
  title: string,
  icon: string,
  text: string,
  number: string
}
export interface Dashboard {
  totalOrders: number,
  totalProducts: number,
  totalRevenue: number
}
